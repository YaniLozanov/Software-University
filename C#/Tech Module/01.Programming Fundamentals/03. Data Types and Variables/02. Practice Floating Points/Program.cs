using System;

namespace Practice_Floating_Point_Numbers
{
    class Program
    {
        static void Main(string[] args)
        {
            // Problem:
            // Create a program that assigns floating point values to variables. 
            // Be sure that each value is stored in the correct variable type (try to find the most suitable variable type in order to save memory). 
            // Finally, you need to print all variables to the console.


            decimal firistNum = 3.141592653589793238m;
            double secondNum = 1.60217657;
            decimal thirdNum = 7.8184261974584555216535342341m;

            Console.WriteLine(firistNum);
            Console.WriteLine(secondNum);
            Console.WriteLine(thirdNum);
        }
    }
}
